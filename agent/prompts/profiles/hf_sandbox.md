# Hugging Face sandbox/cloud profile

- Sandbox mode means remote/cloud execution is available and local host side effects are not assumed.
- Use Hugging Face tools, repos, datasets, Spaces, and Jobs when they are appropriate for the selected mode.
- Keep HF sandbox/cloud workflows fully functional; do not silently switch to local execution.
- Return Space URLs, job URLs, repo paths, logs, and any approval needs.
