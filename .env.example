# ── Codex OAuth ──────────────────────────────────────────────────────────────
# OpenAI Codex CLI authentication — piggybacks on `codex login --device-auth`.
# Codex stores its own token at ~/.config/codex/auth.json after login.
# Set CODEX_AUTH_TOKEN here if you want ml-intern to reuse that session.
# Alternatively, just run `codex login --device-auth` in a terminal first.
# CODEX_AUTH_TOKEN=

# ── HF Token Plan Providers ────────────────────────────────────────────────
# MiniMax Token Plan: https://platform.minimax.io/user-center/basic-information/interface-key
#   Route: MiniMaxAI/MiniMax-M2.7, MiniMaxAI/MiniMax-M2.5 → api.minimax.io/anthropic
MINIMAX_API_KEY=your_minimax_api_key_here

# Z.ai dev platform: https://docs.z.ai/devpack/faq
#   Route: zai-org/GLM-5.1, zai-org/GLM-4.6 → api.z.ai/anthropic/v1
ZAI_API_KEY=your_zai_api_key_here

# ── HuggingFace ────────────────────────────────────────────────────────────────
# Required for dataset/hub access. Get one at https://huggingface.co/settings/tokens
HF_TOKEN=your_hf_token_here

# ── Anthropic / OpenAI (for direct anthropic/ and openai/ prefixes) ────────
# Only needed for models prefixed with anthropic/ or openai/
# ANTHROPIC_API_KEY=sk-ant-api03-...
# OPENAI_API_KEY=sk-...
